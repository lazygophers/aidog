---
title: 贡献流程
description: 修改 aidog 代码和中文文档时执行最小同步、检查与提交流程。
---

# 贡献流程

1. 修改前定位真实调用路径和已有约定。
2. UI、接口或行为变化时同步对应中文页面。
3. 涉及 Command 时重新生成字典。
4. 运行 `yarn check:docs`，并按代码范围运行前端或 Rust 测试。
5. 检查提交中没有凭证、本机数据、生成缓存或无关文件。

源码引用使用仓库相对路径，避免绑定分支的远程 blob 链接。
