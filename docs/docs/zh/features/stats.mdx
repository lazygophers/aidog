---
title: 统计
description: 查看 Token、费用、延迟、模型和分组维度的使用趋势。
---

# 统计

import { ProductDemo } from '../../../theme/components/ProductDemo';

<ProductDemo module="stats" state="normal" />

## 当前实现

侧栏的 `stats` 入口挂载 `src/pages/Stats.tsx`。统计数据由 `src/services/api/stats.ts` 和后端 `platform_cmd::stats` 相关命令提供。

页面只应说明当前仓库实际存在的统计口径：请求数、Token、费用、模型、分组、延迟和聚合项。任何分段、导出或过滤行为以当前源码为准。

## 文档演示边界

上方演示是固定 fixture，不读取真实统计数据。
