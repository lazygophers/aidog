---
title: 模型映射
description: 了解请求模型、平台模型与时段模型如何决定实际模型。
---

# 模型映射

模型映射让客户端使用稳定名称，平台转发到各自实际模型。决策顺序为：用户显式 `time_models`、命中高峰时的预设 `models.peak`、平台模型映射。

高峰模型是预设级约束，会覆盖平台手工模型；要保留自定义行为应配置 `time_models`。模型列表只是编辑候选，不代表上游账号一定有权限。
