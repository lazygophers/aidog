---
title: 数据与隐私
description: 了解本机数据、日志粒度、脱敏规则和备份边界。
---

# 数据与隐私

配置、分组、日志和设置存入本机 SQLite；请求仍会转发给你配置的上游。

日志有总开关、用户原始请求和上游请求三层控制。关闭某侧后，该侧 headers 与 body 不入库，只保留模型、Token、费用、URL、状态等元数据。Authorization 等敏感头会脱敏。原始信息到期清空字段，整行保留期到期才删除记录。

加密 `.aidogx` 备份仍可能包含凭证，应限制访问且不得提交到版本库。
