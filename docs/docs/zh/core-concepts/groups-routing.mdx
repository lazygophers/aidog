---
title: 分组与路由
description: 理解分组鉴权、候选过滤、调度策略和请求隔离。
---

# 分组与路由

分组把客户端身份、模型规则和候选平台组合起来。Bearer token 用作分组名，路由器只在该分组关联的平台中选择候选。

候选受启用状态、过期时间、高峰禁用、熔断、模型和端点约束，再按顺序、随机或加权策略排序。整组无候选时请求失败，不会越过分组。高峰禁用不会因单平台组而绕过。
