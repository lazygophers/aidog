---
title: Référence API
---

# Référence API

AiDog expose une API HTTP locale pour interroger les informations de groupe. Tous les points de terminaison sont **uniquement localhost** (`127.0.0.1`) et ne nécessitent aucun accès réseau externe.

## POST /api/group-info

Interroge les informations estimées d'un groupe (solde, utilisation du Coding Plan, statistiques de requêtes, etc.). Seuls les **groupes à plateforme unique** renvoient des données applicables ; les groupes multi-plateformes ou sans plateforme renvoient `applicable: false`.

### Authentification

```
Authorization: Bearer <group_name>
```

Le `group_name` est le nom du groupe configuré dans AiDog. Il s'agit de la même valeur que la variable d'environnement `ANTHROPIC_AUTH_TOKEN` utilisée par Claude Code.

### Requête

- **Méthode** : POST
- **URL** : `http://127.0.0.1:<port>/api/group-info`
- **En-têtes** : `Authorization: Bearer <group_name>`

### Réponse (200 OK)

#### Groupe à plateforme unique :

```json
{
  "applicable": true,
  "balance": 48.20,
  "spent": 1.23,
  "coding_plan": [
    {
      "name": "5h",
      "utilization": 23,
      "pace": "fast",
      "reset_at": null
    },
    {
      "name": "7d",
      "utilization": 41,
      "pace": "normal",
      "reset_at": null
    }
  ],
  "requests": 42,
  "success_rate": 95.2,
  "cache_rate": 13.3,
  "total_tokens": 123456,
  "currency": "",
  "balance_days_remaining": 5.2
}
```

#### Groupe multi-plateformes / sans plateforme :

```json
{
  "applicable": false,
  "balance": 0.0,
  "spent": 0.0,
  "coding_plan": [],
  "requests": 0,
  "success_rate": 0.0,
  "cache_rate": 0.0,
  "total_tokens": 0,
  "currency": "",
  "balance_days_remaining": null
}
```

### Champs de réponse

| Champ | Type | Description |
|-------|------|-------------|
| `applicable` | boolean | Indique s'il s'agit d'un groupe à plateforme unique avec des données disponibles |
| `balance` | number | Solde restant estimé |
| `spent` | number | Dépense estimée cumulée |
| `coding_plan` | array | Paliers Coding Plan (Claude, etc.) ; vide pour les plateformes basées sur le solde |
| `coding_plan[].name` | string | Nom du palier (5h, 7d, etc.) |
| `coding_plan[].utilization` | number | Pourcentage d'utilisation (0-100) |
| `coding_plan[].pace` | string | Taux de consommation : `fast`, `normal` ou `busy` |
| `coding_plan[].reset_at` | number\|null | Horodatage de réinitialisation (secondes Unix) |
| `requests` | number | Nombre total de requêtes |
| `success_rate` | number | Taux de réussite (0-100) |
| `cache_rate` | number | Taux de réussite du cache (0-100) |
| `total_tokens` | number | Total des tokens consommés |
| `currency` | string | Unité monétaire |
| `balance_days_remaining` | number\|null | Nombre estimé de jours de solde restants (null si incalculable) |

### Réponses d'erreur

| Statut | Condition |
|--------|-----------|
| 401 | En-tête `Authorization` manquant ou invalide |
| 200 | Groupe introuvable → `applicable: false` |

### Exemple

```bash
curl -X POST \\
  -H "Authorization: Bearer my-group" \\
  http://127.0.0.1:9876/api/group-info
```
