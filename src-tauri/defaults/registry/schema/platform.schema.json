{
  "$schema": "http://json-schema.org/draft-07/schema#",
  "$id": "platform.schema.json",
  "title": "platform preset",
  "description": "platforms/<code>/platform.json。构建期由 build.rs include_str! 内置，运行期 DB 空表时兜底（ADR 0005）。",
  "type": "object",
  "additionalProperties": false,
  "definitions": {
    "locale_text": {
      "type": "object",
      "additionalProperties": false,
      "required": [
        "en-US",
        "zh-Hans",
        "ar-SA",
        "fr-FR",
        "de-DE",
        "ru-RU",
        "ja-JP",
        "es-ES"
      ],
      "properties": {
        "en-US": {
          "type": "string",
          "minLength": 1,
          "description": "【必填】英语文案"
        },
        "zh-Hans": {
          "type": "string",
          "minLength": 1,
          "description": "【必填】简体中文文案"
        },
        "ar-SA": {
          "type": "string",
          "minLength": 1,
          "description": "【必填】阿拉伯语文案（RTL）"
        },
        "fr-FR": {
          "type": "string",
          "minLength": 1,
          "description": "【必填】法语文案"
        },
        "de-DE": {
          "type": "string",
          "minLength": 1,
          "description": "【必填】德语文案"
        },
        "ru-RU": {
          "type": "string",
          "minLength": 1,
          "description": "【必填】俄语文案"
        },
        "ja-JP": {
          "type": "string",
          "minLength": 1,
          "description": "【必填】日语文案"
        },
        "es-ES": {
          "type": "string",
          "minLength": 1,
          "description": "【必填】西班牙语文案"
        }
      }
    }
  },
  "required": [
    "last_updated",
    "endpoints",
    "models",
    "model_list",
    "name",
    "source_urls",
    "homepage",
    "logo_url",
    "keywords",
    "color"
  ],
  "properties": {
    "last_updated": {
      "type": "integer",
      "minimum": 0,
      "description": "【必填】内容变更时间 Unix 秒。改完内容必须跑 scripts/bump-registry-last-updated.mjs 重新盖戳，不盖则远程同步按内容比对永远跳过该文件。"
    },
    "is_coding_plan": {
      "type": "boolean",
      "description": "【可选】编码套餐（coding plan）协议标记：true = 整套协议走 coding 套餐（独立子域 / 配额计费）。router/ordering.rs 优先普通平台；前端 ProtocolOption.codingPlan / PlatformCard 徽标据此派生。coding 套餐一律是独立协议条目（禁与普通版同 code 双 preset）。"
    },
    "key_prefixes": {
      "type": "array",
      "minItems": 1,
      "items": {
        "type": "string",
        "minLength": 2
      },
      "description": "【可选】平台 API key 前缀（如 sk-ant- / sk-kimi- / tp- / ark-）。粘贴识别的 key 提取与平台直判（platformPaste.ts 优先级 2）据此数据驱动生成，禁在代码硬编码平台前缀；通用格式前缀（sk- / sk_）仍留代码。coding 套餐平台是独立协议（禁与普通版同 code 双 preset），其专属 token 前缀写在本字段，不另设 coding 前缀字段。"
    },
    "endpoints": {
      "type": "object",
      "additionalProperties": false,
      "required": [
        "default"
      ],
      "properties": {
        "default": {
          "type": "array",
          "items": {
            "type": "object",
            "additionalProperties": false,
            "required": [
              "protocol",
              "base_url"
            ],
            "properties": {
              "protocol": {
                "type": "string",
                "description": "【必填】端点 wire 协议（= Rust Protocol 枚举头段 5 个请求协议，与前端 constants.ts PROTOCOLS 对称）。平台类型协议（glm_coding / mock / devin 等）不作为 endpoint 协议。",
                "enum": [
                  "anthropic",
                  "openai",
                  "openai_responses",
                  "openai_completions",
                  "gemini"
                ]
              },
              "base_url": {
                "type": "string",
                "pattern": "^https?://",
                "description": "【必填】含版本前缀（如 /v1、/api/paas/v4）。最终 URL = base_url + provider_api_path，禁止额外拼接。"
              },
              "client_type": {
                "type": "string",
                "enum": [
                  "default",
                  "claude_code",
                  "claude_code_vscode",
                  "claude_code_sdk_ts",
                  "claude_code_sdk_py",
                  "claude_code_gh_action",
                  "codex_cli",
                  "codex_tui",
                  "codex_desktop",
                  "codex_vscode",
                  "pi_cli",
                  "cursor",
                  "windsurf"
                ],
                "description": "【可选】可选：端点客户端形态覆盖。缺省按 protocol 派生（前端 defaults.ts::clientTypeForProtocol / Rust registry.rs::derive_client_type 对称）：anthropic → claude_code、openai 系 → codex_tui、其余 → default。仅派生规则不适用的例外平台标注（如官方 claude_code 直连端点标 default 不模拟客户端）。"
              },
              "coding_plan": {
                "type": "boolean",
                "description": "【可选】标记该端点属编码套餐（true 时路由走 cp 端点路径）。"
              }
            }
          },
          "description": "【必填】端点列表（每项含 protocol / base_url / client_type）。"
        }
      },
      "description": "【必填】端点表，仅 default 单分支。coding 套餐是独立协议（如 glm_coding / minimax_coding），不再有 coding_plan 分支。"
    },
    "models": {
      "type": "object",
      "additionalProperties": false,
      "required": [
        "default"
      ],
      "properties": {
        "default": {
          "type": "object",
          "additionalProperties": {
            "type": "string",
            "minLength": 1
          },
          "description": "【必填】默认槽位映射（槽位：default / opus / sonnet / haiku / gpt / fable → 平台真实模型 id）。聚合/中转平台可留空对象。"
        },
        "peak": {
          "type": "object",
          "additionalProperties": {
            "type": "string",
            "minLength": 1
          },
          "description": "【可选】高峰时段模型切换分支（PRD 07-11，现仅 glm_coding）。命中平台 peak 任一窗口时整支替换 default（router/candidates.rs 三层级联第 ② 层）。"
        }
      },
      "description": "【必填】客户端模型槽位映射表。default 必填；peak 高峰切换（现仅 glm_coding）。coding 套餐是独立协议，无 coding_plan 分支。"
    },
    "model_list": {
      "type": "object",
      "additionalProperties": false,
      "required": [
        "default"
      ],
      "properties": {
        "default": {
          "type": "array",
          "items": {
            "type": "string",
            "minLength": 1
          },
          "description": "【必填】下拉冷启动候选全集（不变量：models 各分支值集的超集）。"
        }
      },
      "description": "【必填】模型下拉候选表，仅 default 单分支（default 必须是 models 各分支值集的超集，冷启动兜底列全候选）。coding 套餐是独立协议，无 coding_plan 分支。"
    },
    "peak": {
      "type": "array",
      "minItems": 1,
      "items": {
        "type": "object",
        "additionalProperties": false,
        "required": [
          "start_hour",
          "end_hour",
          "multiplier"
        ],
        "properties": {
          "start_hour": {
            "type": "integer",
            "minimum": 0,
            "maximum": 23,
            "description": "【必填】窗口起始小时（窗口时区本地，0-23；跨天窗口 end < start）。"
          },
          "end_hour": {
            "type": "integer",
            "minimum": 0,
            "maximum": 23,
            "description": "【必填】窗口结束小时（窗口时区本地，0-23；end=24 语义为到 23 点末）。"
          },
          "multiplier": {
            "type": "number",
            "exclusiveMinimum": 0,
            "description": "【必填】窗口内计费倍率（条目带模型 peak 绝对价时被压成 1.0）。"
          },
          "timezone": {
            "type": "string",
            "minLength": 1,
            "description": "【可选】窗口时区（IANA 名，如 Asia/Shanghai）；小时/星期按该时区本地时刻解释；缺省 = UTC。"
          },
          "days_of_week": {
            "type": "array",
            "minItems": 1,
            "items": {
              "type": "integer",
              "minimum": 0,
              "maximum": 6
            },
            "description": "【可选】窗口限定的星期（窗口时区本地，0=周日 … 6=周六）；缺省 = 每天。"
          },
          "models": {
            "type": "array",
            "minItems": 1,
            "items": {
              "type": "string",
              "minLength": 1
            },
            "description": "【可选】限定仅特定请求模型命中窗口；缺省 = 该平台全部模型。"
          },
          "start_at": {
            "type": "integer",
            "minimum": 0,
            "description": "【可选】窗口生效起点（Unix 秒）。"
          }
        }
      },
      "description": "【可选】平台级高峰/低峰计价窗口（每窗口可指定 timezone，缺省 UTC；first-match wins，跨天 end<start）。条目带模型 peak 绝对价时本倍率被压成 1.0。注意三层同名不同层：本字段 peak = 平台级计价窗口；models.peak = 高峰模型切换分支；模型条目 peak = 高峰绝对价对象。"
    },
    "name": {
      "type": "object",
      "additionalProperties": false,
      "required": [
        "en-US",
        "zh-Hans",
        "ar-SA",
        "fr-FR",
        "de-DE",
        "ru-RU",
        "ja-JP",
        "es-ES"
      ],
      "properties": {
        "en-US": {
          "type": "string",
          "minLength": 1,
          "description": "【必填】英语显示名"
        },
        "zh-Hans": {
          "type": "string",
          "minLength": 1,
          "description": "【必填】简体中文显示名"
        },
        "ar-SA": {
          "type": "string",
          "minLength": 1,
          "description": "【必填】阿拉伯语显示名（RTL）"
        },
        "fr-FR": {
          "type": "string",
          "minLength": 1,
          "description": "【必填】法语显示名"
        },
        "de-DE": {
          "type": "string",
          "minLength": 1,
          "description": "【必填】德语显示名"
        },
        "ru-RU": {
          "type": "string",
          "minLength": 1,
          "description": "【必填】俄语显示名"
        },
        "ja-JP": {
          "type": "string",
          "minLength": 1,
          "description": "【必填】日语显示名"
        },
        "es-ES": {
          "type": "string",
          "minLength": 1,
          "description": "【必填】西班牙语显示名"
        }
      },
      "description": "【必填】8 locale 本地化显示名，全部必填非空。"
    },
    "source_urls": {
      "type": "object",
      "additionalProperties": false,
      "required": [
        "docs"
      ],
      "properties": {
        "docs": {
          "type": "string",
          "minLength": 1,
          "description": "【必填】官方文档 URL（必填）。"
        },
        "pricing": {
          "type": "string",
          "minLength": 1,
          "description": "【可选】官方定价页。除 devin（无公开定价页）外全部平台必填。"
        }
      },
      "description": "【必填】官方文档/定价页 URL（docs 必填；pricing 除 devin 外全部必填）。"
    },
    "homepage": {
      "type": "string",
      "minLength": 1,
      "description": "【必填】平台官网 URL。"
    },
    "logo_url": {
      "type": "string",
      "description": "【必填】Simple Icons slug（如 minimax）或完整图片 URL，空串走 favicon/clearbit/首字母 fallback。本项目不存储任何平台 logo，由用户端从第三方获取（logo_sync.rs 三路拉取）。"
    },
    "keywords": {
      "type": "array",
      "minItems": 1,
      "items": {
        "type": "string",
        "minLength": 1
      },
      "description": "【必填】搜索关键词（platformPaste.ts 粘贴匹配 + 前端搜索）。词条多语言：中文、英文品牌名/别名，中文词条须附全拼串与首字母串（如 智谱 → zhipu + zp）、日语假名词条须附罗马音（Hepburn，如 エヌビディア → enubidia）——拼音/罗马音等音译转写一律作为数据存于此处，代码不做推导。子平台变体须包含父平台全部词条。"
    },
    "color": {
      "type": "string",
      "pattern": "^#[0-9A-Fa-f]{6}$",
      "description": "【必填】品牌色，前端徽标/卡片着色。"
    },
    "quota_scripts": {
      "type": "array",
      "minItems": 1,
      "items": {
        "type": "object",
        "additionalProperties": false,
        "required": [
          "id",
          "name",
          "script"
        ],
        "properties": {
          "id": {
            "type": "string",
            "minLength": 1,
            "description": "【必填】部署变体 id（用户选择存 platform.extra.quota_script_id；缺省取数组首条，id 消失/删条回落首条）。同平台内唯一。"
          },
          "name": {
            "$ref": "#/definitions/locale_text",
            "description": "【必填】变体显示名（8 locale，如 new-api 官方 / done-hub / VoAPI 等 fork 名）。"
          },
          "requires": {
            "type": "array",
            "items": {
              "type": "object",
              "additionalProperties": false,
              "required": [
                "key",
                "label"
              ],
              "properties": {
                "key": {
                  "type": "string",
                  "minLength": 1,
                  "description": "【必填】参数键：值由用户填写，存 platform.extra.<key>，脚本经 ctx.extra 读取（extra 是 JSON 字符串，脚本内 JSON.parse）。"
                },
                "label": {
                  "$ref": "#/definitions/locale_text",
                  "description": "【必填】参数输入框标签（8 locale，前端据此自动渲染 requires 表单）。"
                }
              }
            },
            "description": "【可选】用户参数声明（前端按选中变体自动渲染输入框）；缺省 []。"
          },
          "returns": {
            "type": "object",
            "additionalProperties": false,
            "properties": {
              "balance": {
                "type": "boolean",
                "description": "【可选】脚本可返回余额（BalanceInfo）；缺省 false。"
              },
              "coding_plan": {
                "type": "boolean",
                "description": "【可选】脚本可返回 Coding Plan 配额（CodingPlanInfo）；缺省 false。"
              },
              "mcp": {
                "type": "boolean",
                "description": "【可选】脚本可返回 MCP 用量（如 GLM TIME_LIMIT 月用量）；缺省 false。"
              },
              "tiers": {
                "type": "array",
                "items": {
                  "type": "string",
                  "enum": [
                    "five_hour",
                    "weekly_limit",
                    "monthly",
                    "mcp_monthly"
                  ]
                },
                "description": "【可选】脚本可返回的配额层级名（QuotaTier.name 词表：five_hour / weekly_limit / monthly / mcp_monthly）；缺省 []。"
              }
            },
            "description": "【可选】能力声明：查询入口与 QuotaCapability 由选中变体的 returns 派生；缺省全 false / []。"
          },
          "script": {
            "type": "string",
            "minLength": 1,
            "description": "【必填】JS 脚本文本（boa 引擎执行，注入 ctx.baseUrl / ctx.apiKey / ctx.extra 与 http.get / http.post；顶层 return 返回固定格式对象）。单脚本自包含：内部可多次调上游接口再汇总。同族协议各自文件复制正文，无跨文件引用。"
          }
        }
      },
      "description": "【可选】配额查询脚本变体数组（每条对应一种部署变体，如 new-api 官方与各 fork）。运行时按用户选中物化单条入 DB 执行；远程同步整份分发但不自动换脚本。"
    }
  },
  "last_updated": 1788012963
}
