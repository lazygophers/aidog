# Pending Tasks (待建, 等 platform-expiry-toggle 闭环后处理)

> 排队原因: platform-expiry-toggle agent a0d7f78c 在跑, `task.py create` 抢 current 致 finish 误中 (memory [[trellis-create-steals-current-task]])。toggle finish/archive 后立即建。

## pending: 分享按钮复制失败 (share-copy-fail)

**用户原话**: 「点击分享平台的按钮，提示复制失败，请修复这个问题」

**初步定位** (只读 grep):
- `src/components/platforms/ShareModal.tsx:54` `await writeText(text)` (Tauri plugin clipboard-manager)
- line 65 catch → toast `platform.share.copyFail` "复制失败，请手动选择内容"
- 已用 Tauri 插件 (非 navigator.clipboard; SmartPasteModal:95 注释说 WKWebView 无手势时 navigator 静默失败)
- 仍失败 → writeText 抛异常, 可能:
  1. Tauri clipboard 权限未配/拒绝
  2. text 为空/undefined (分享内容生成失败)
  3. 插件未正确安装/初始化
  4. WKWebView 安全上下文 (非 HTTPS / file://) 限制

**另发现**: `Platforms.tsx:2783` 另一处 `navigator.clipboard.writeText(apiKey)` (非 share 按钮, apiKey 复制) — 可能也 broken (WKWebView navigator 静默失败), 顺带查/修。

**待 agent 诊断**:
- 复现: 打开分享弹窗 → 点复制 → 看是否 toast 失败 + 控制台报错
- 查 writeText 抛什么异常 (try/catch 打印 error)
- 查 text 生成逻辑 (ShareModal 构造分享文本是否成功)
- 查 Tauri clipboard-manager 插件配置 (capabilities/permissions)
- 对比 SmartPasteModal.tsx (readText 正常) 的配置差异

**建议 slug**: `share-copy-fail` 或 `fix-share-clipboard`

**建 task 时机**: platform-expiry-toggle finish/archive 后

---

## pending: skills 列表读全局锁文件 (skills-list-lockfile)

**用户原话** (2026-06-25 澄清): 「我希望skills的列表信息（已安装的、全局的）直接从 ~/.agents/.skill-lock.json 读取，不经过npx skills 命令，但是搜索、安装、移除、更新、启用/禁用还是需要经过命令的。会全局的也还是通过命令」

**范围澄清 (用户明确界定)**:
| 操作 | 数据源 |
|---|---|
| **列表读取** (已装 / 全局) | **直接读 `~/.agents/.skill-lock.json`** (免 npx, 快) |
| **搜索** | npx 命令 |
| **安装 / 移除 / 更新** | npx 命令 |
| **启用 / 禁用** | npx 命令 |
| **全局列表** | npx 命令 (用户: 「会全局的也还是通过命令」— 全局 catalog 浏览走 npx find, 非锁文件) |

**现状**:
- `src-tauri/src/gateway/skills/list.rs:13` 走 `npx skills list --json` (shell out, 慢) 解析 + 锁文件 source 富化
- `~/.agents/.skill-lock.json` 已含全部已装 skill: name/source/sourceType/sourceUrl/skillPath/hash/pluginName/installedAt/updatedAt
- memory [[skills-catalog-dual-source]] [[npx-skills-cli]] [[skills-management-module]] [[skills-list-lockfile]]

**待 agent planning 时澄清 (brainstorm 问用户)**:
- per-agent enable 状态来源: 锁文件无 agents[] 字段。列表展示 enable 状态怎么拿?
  - 候选 A: 列表只展示锁文件字段 (name/source/sourceType/skillFolderHash), enable 状态不展示或延迟加载
  - 候选 B: enable 状态走本地 config 文件读 (~/.claude/skills + ~/.codex symlink 探测, 免 npx)
  - 候选 C: enable 状态仍走 npx 但缓存 (锁文件给基础 + npx 补 enable, 慢路径降级)
- 注意用户说「启用/禁走命令」= **写**操作走 npx; **读** enable 状态是否也走 npx 待澄清 (读频繁, 走 npx 慢)
- 展示需求: source / sourceType / skillFolderHash 字段渲染 (锁文件已有)

**建议 slug**: `skills-list-lockfile`
**优先级**: P2 (与 P0 skills-removal-deep-audit 不同任务域, 不冲突可并行, 但用户未要求立即)
**建 task 时机**: skills-removal-deep-audit 闭环后 (避免 create 抢 current)

---

## pending: SkillInstallView 交互重设计 (skills-install-ux-redesign)

**合并三个用户反馈** (全属 `src/pages/SkillInstallView.tsx` 交互):

### Bug 1: 搜索 loading 期间显旧数据
用户原话:「在搜索等待的过程中，直接展示了数据，而不是加载中的样式，会引起用户误会」
- line 42 `loading` state, line 158-160 搜索中样式
- 问题: loading=true 期间旧 results 仍渲染, 用户误以为搜完
- 修复方向: loading 时隐藏 results (`!loading` 条件) 或清空旧 results 或叠加 skeleton

### Bug 2: 已安装 skill 仍显安装按钮
用户原话:「已安装的没有显示已安装还是提供了安装的按钮」
- line 25 `installedNames: Set<string>` prop
- line 229 `const already = installedNames.has(entry.name)` — **有判断逻辑**
- line 259/298 "已装" 文本展示
- 问题: 有 already 判断却仍显安装按钮 → 可能:
  1. installedNames 未正确传入 (Skills.tsx 父组件没传最新已装列表)
  2. name 匹配错 (catalog entry.name vs installed skill name 不一致)
  3. already 判断后条件渲染仍走安装分支 (bug)
- agent 查 line 229-298 渲染分支 + Skills.tsx 传 installedNames 处

### Bug 3: 并发点击丢安装中状态
用户原话:「连续点击两个安装按钮，第一个安装还没有完成就取消了安装中的状态展示」
- `busyId` 单值 state (line 155 `busyId !== null` disabled 判断)
- 点第二个 setBusyId(id2) 覆盖 id1 → 第一个按钮恢复非 busy
- 修复方向:
  - 方案 A: busyIds Set 支持多并发安装
  - 方案 B: 安装期间禁用其他按钮 (busyId!=null 时所有按钮 disabled) — 更简单, 防并发混乱

**建议 slug**: `skills-install-ux-redesign`
**建议**: 三 bug 合一 task (同页同组件), agent 统一重设计 SkillInstallView 交互。

**建 task 时机**: platform-expiry-toggle finish/archive 后

---

## pending: skills 测试隔离 (skills-test-isolation) [P0 高优先]

**用户原话**: 「似乎执行单元测试或者别的检查的时候，会直接操作系统的skills，预期单元测试应该是独立的、隔离的、不影响用户设备的」

**根因** (已定位): `src-tauri/src/gateway/skills/test_bulk.rs`
- `align_agents_different_agents_does_not_panic` (line 21): 真实调 `align_agents(Claude, Codex, Global, None)` → shell out `npx skills enable/disable` 操作用户 `~/.agents/.skill-lock.json`
- `enable_all_does_not_panic` (line 29): 真实调 `enable_all(Claude, Global, None)` → 操作用户 skills
- 注释自承 "May succeed or fail depending on installed skills" = 操作真实环境
- `align_agents_same_agent_noop` (line 13) from==to noop 安全

**安全 (不 shell out)**: `test_env.rs` / `test_proxy_env.rs` 的 `Command::new("npx")` 只构造 cmd 测 env 注入, 不真实执行。

**修复方向** (agent 定):
- 方案 A: 测试设 `HOME=tempdir` (scoped fixture) → 操作落到临时目录, 不碰用户 `~/.agents`
- 方案 B: `align_agents` / `enable_all` 接受可注入 cmd builder / scope, 测试传 `SkillScope::Project { path: tempdir }`
- 方案 C: 标 `#[ignore]` 转集成测试 (cargo test 默认跳过)
- 推荐 A (最小改动, 真隔离): 测试 fn 内 `std::env::set_var("HOME", &tempdir)` + 用 tempdir fixture, 跑完清理

**严重性**: 每次跑 cargo test 真实操作用户 ~/.agents skills — **必须修**, 测试隔离硬要求。

**建议 slug**: `skills-test-isolation`
**优先级**: P0 (platform-expiry-toggle finish 后**第一个**处理)

---

## pending: 粘贴「6.27到期」未识别过期时间 (expiry-paste-not-recognized)

**用户原话**: 「没有识别到过期时间」+ 提供测试样本 (论坛帖):
```
分享一个MIMO key，PRO套餐，6.27到期
...
base url
兼容 OpenAI 接口协议：https://token-plan-cn.xiaomimimo.com/v1
兼容 Anthropic 接口协议：https://token-plan-cn.xiaomimimo.com/anthropic

key
tp-caxzn1uh0ck6f46btdxr35o6hyb9a5aa25uk93yh4u1n628k
```

**预期**: 识别「6.27到期」→ 过期时间 2026-06-27 23:59:59 (今天 2026-06-25, 按 platform-expiry-toggle 约定当日结束; memory [[extractexpiryat-false-positive-fallback]])

**根因推测** (待 agent 定):
- `src/utils/platformPaste.ts` extractExpiryAt / parseCandidate:
  1. `M.D` (月.日, 无年份) 格式正则未覆盖 — 现有 DATETIME_RE 可能只认 YYYY-MM-DD / M月D日
  2. 「到期」语义词与日期「6.27」字符距离 > 60 门槛 (memory [[extractexpiryat-false-positive-fallback]] 加的门槛)
  3. 中文标点 「，」分隔影响 tokenization
- memory 关联: [[extractexpiryat-false-positive-fallback]] [[platform-smart-paste-parser]] [[platform-expiry-toggle]]

**待 agent 诊断**:
- 复现: 把样本贴进添加平台智能粘贴, 看是否填了过期时间 (应填 2026-06-27 23:59:59 但 toggle OFF)
- grep platformPaste.ts 看日期正则是否支持 `M.D` 格式
- 测「到期」「到期时间」「过期」等语义词是否在关键词表
- 补 `M.D` / `M月D日` / `M/D` 格式 + 无年份时取当前年 (跨年取下年)
- 加 vitest case 覆盖本样本

**建议 slug**: `expiry-paste-not-recognized`
**优先级**: P1 (用户刚反馈, 但非破坏性; 排 skills-test-isolation 闭环后)
**建 task 时机**: skills-test-isolation finish/archive 后

---

## pending: router 同优先级按过期时间最早调度 (platform-expiry-priority)

**用户原话** (2026-06-26): 「同优先级的情况下，优先使用过期时间最早的」+「同优先级的情况下，优先调度最旧的」
**语义澄清** (AskUserQuestion 已确认): 「最旧」= 过期时间最早 (expiry_at 升序), 非创建时间/更新时间

**需求**:
- router.rs 平台选择: 同优先级 (priority) 的候选平台中, 按 expiry_at 升序选 (快到期优先)
- 目的: 优先用快过期平台, 避免额度浪费

**现状待研究**:
- `src-tauri/src/gateway/router.rs` select_platform / 候选排序逻辑
- platform expiry_at 字段 (platform-expiry-toggle 加, memory [[extractexpiryat-false-positive-fallback]])
- 无 expiry_at 的平台 (未设过期) 排序位置 (放最后? 或视为无穷大?)
- 与现有 tiebreaker (attempts/breaker/balance) 的优先级关系

**建议 slug**: `platform-expiry-priority`
**优先级**: P2 (调度优化, 非破坏性)
**建 task 时机**: skills-list-lockfile 闭环后

---

## 排队队列汇总 (等 skills-test-isolation finish)

1. **datetime-local-theme** (task B, prd/implement 就绪, 依赖已满足) — datetime-local CSS 染色 + indicator 滤镜
2. **expiry-paste-not-recognized [P1]** — 粘贴「6.27到期」未识别, 补 M.D 格式 + 语义词
3. **share-copy-fail** — ShareModal.tsx:54 writeText 抛异常 + Platforms.tsx:2783 navigator.clipboard
4. **skills-list-lockfile** — 读 ~/.agents/.skill-lock.json 免 npx + 展示 source/sourceType/skillFolderHash
5. **skills-install-ux-redesign** — SkillInstallView 三 bug 合一: loading 显旧数据 / 已装仍显安装按钮 / busyId 单值并发丢状态

**建议处理顺序**: datetime-local-theme → expiry-paste (用户刚反馈) → skills 两个 → share-copy-fail。或用户指定。
